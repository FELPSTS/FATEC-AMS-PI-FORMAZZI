import React from "react";
import { useNavigate, BrowserRouter as Router } from "react-router-dom";
import "./content.css"

export default function _IntroductionContent() {
    let navigate = useNavigate();

    return (
        <div>
            <p>alooooooo</p>
        </div>
    );
}